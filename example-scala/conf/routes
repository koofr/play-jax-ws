# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /example-service*path          controllers.ExampleServiceController.get(path)
POST    /example-service*path          controllers.ExampleServiceController.post(path)
